# Aimo Pulse — Supabase project config
project_id = "iqwfevbjeohdvdbemgxp"

[functions.make-server-494d91eb]
verify_jwt = false

# REQUIRED for push when the app is closed:
# Dashboard → Edge Functions → make-server-494d91eb → Schedules → */15 * * * * (every 15 min)
# Method: POST  Path: /calendar/process-reminders
# Header: X-Cron-Secret = value of CRON_SECRET
# Sends calendar reminders (0–5 days out) + morning/midday/evening pulse reminders via FCM
